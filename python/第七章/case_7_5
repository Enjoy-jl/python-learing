#case 7-5
age = ""
while age != "quit":
	age = input("Hi,how old are you?: ")
	if age != "quit": 
		age = int(age)
		if age <3:
			print (f"you can pay 0 $")
		elif age>=3 and age <= 12:
			print (f"you have pay 10 $")
		else :
			print (f"you have pay 15 $")